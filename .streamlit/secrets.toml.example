# Copy this file to secrets.toml (same folder) for local testing.
# secrets.toml is gitignored - never commit your real password.
# On Streamlit Community Cloud, set this same key under the app's
# Settings -> Secrets instead of using a file.

APP_PASSWORD = "choose-a-real-password-here"
